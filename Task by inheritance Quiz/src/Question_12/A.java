package Question_12;

public class A {
	
		// que- You know that compiler will keep super() calling statement implicitly as a first statement in every constructor. What happens if we write this() as a first statement in our constructor?

		// ans- Compiler will not keep super() if you are writing this() as a first statement in your constructor.
}
